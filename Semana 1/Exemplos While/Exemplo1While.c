#include <stdio.h>

int main()
{
    // Contagem até 60
    int num;
    num = 0;
    while (num < 60)
    {
        num = num + 1;
        printf("%d\n", num);
    }
    return (0);
}
