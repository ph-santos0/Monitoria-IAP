#include <stdio.h>

int main()
{
    // Contagem até 60
    int num;
    num = 0;
    while (num <= 60)
    {
        printf("%d\n", num);
        num = num + 1;
    }
    return (0);
}
