#include <stdio.h>

int main()
{
    int goleAgua = 0;

    while (goleAgua < 5)
    {
        goleAgua = goleAgua + 1;
        printf("Bebendo 1 gole de agua, total de goles atuais: %d\n", goleAgua);
    }
    return (0);
}
